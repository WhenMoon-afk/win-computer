# @whenmoon-afk/win-computer

Remote Windows desktop over HTTP MCP for Oh My Pi.

Windows, in OMP: `/win-computer host`
Other machine: `/win-computer join <url>`
Then `/mcp reload`. Tools are `mcp__win_computer_*`. Not local `computer.*`.

See the marketplace README in the repo root.
